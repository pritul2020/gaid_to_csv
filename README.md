# MAC or LINUX Commands
python3 -m venv venv
source venv/bin/activate
python app.py

# Windows Command 
python -m venv venv
venv\Scripts\activate
python app.py

# Move the gaid file into the directory and write its name in app.py and just convert it .